# Required: one key for API auth and Flask sessions (SECRET_KEY is the same value)
API_KEY=change-me-to-a-long-random-string

HOST=0.0.0.0
PORT=7860

# Hugging Face model (weights stored under ./models by default)
MODEL_ID=Qwen/Qwen3-Omni-30B-A3B-Instruct
# MODELS_DIR=./models
# MODEL_PATH=./models/Qwen3-Omni-30B-A3B-Instruct

# transformers | vllm
INFERENCE_BACKEND=transformers
LOAD_MODEL_ON_STARTUP=true
MOCK_INFERENCE=false

# Multi-GPU (g5.12xlarge: 4x A10G 24GB) — see README "g5.12xlarge"
TENSOR_PARALLEL_SIZE=4
MAX_MEMORY_PER_GPU=22GiB
DISABLE_CPU_OFFLOAD=true
RETURN_AUDIO=false
# VLLM_GPU_MEMORY_UTILIZATION=0.85
# VLLM_MAX_MODEL_LEN=32768

# Generation defaults
MAX_NEW_TOKENS=2048
TEMPERATURE=0.7
TOP_P=0.95
USE_AUDIO_IN_VIDEO=true
DEFAULT_SPEAKER=Ethan

UPLOAD_FOLDER=/tmp/qwen-uploads
MAX_CONTENT_LENGTH=104857600

# Hugging Face token (if model download requires auth)
# HF_TOKEN=hf_xxx
